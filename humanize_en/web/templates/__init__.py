"""Jinja2 template directory for humanize-en's HTMX UI.

This file exists so hatchling treats ``templates/`` as a
sub-package of ``humanize_en.web`` and includes the rendered
``*.html`` files in the wheel (mirroring the ZH plugin's pattern).
The templates themselves are stub placeholders during M11 step 1
and get filled in with the ported humanize-zh templates in M11
steps 2-3.
"""
